package:
    name: torchcule
    version: 0.1

source: 
    path: ..

requirements:
    build:
        - libgcc-ng
        - libstdcxx-ng
        - python
        - setuptools
        - cython
        - pybind11
        - zlib
    run:
        - libstdcxx-ng
        - libgcc-ng
        - zlib
        - numpy >=1.11
        - atari_py 
        - gym
        - pytorch >=1.4
        - tqdm

test:
    imports:
        - torchcule.atari
    commands:
        - python -c "from torchcule.atari import Env; env = Env('PongNoFrameskip-v4', 1); env.reset()"